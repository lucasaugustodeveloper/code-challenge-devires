import * as React from 'react';

import * as S from './styled';

import propTypes from './types';

const Header = ({ children }: propTypes) => <S.Header>{children}</S.Header>;

export default Header;
